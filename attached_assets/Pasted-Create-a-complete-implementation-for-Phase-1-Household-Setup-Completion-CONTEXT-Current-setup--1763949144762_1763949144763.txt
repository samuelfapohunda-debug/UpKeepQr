Create a complete implementation for Phase 1: Household Setup Completion.

CONTEXT:
- Current setup flow creates household but leaves it incomplete
- Setup status stays 'in_progress', no home profile saved
- QR code activation status never updated
- No link between household and original order

REQUIREMENTS:

1. TRANSACTION WRAPPER
   - Wrap all database operations in db.transaction()
   - Ensure atomicity: all succeed or all rollback
   - Location: server/src/routes/setup.ts, POST /activate endpoint

2. DUPLICATE CHECK
   - Before creating household, check if magnetToken already exists in households table
   - Query: SELECT * FROM households WHERE magnet_token = ?
   - If exists: return 409 error "Activation code already used"
   - Include householdId in error for reference

3. LINK TO ORDER
   - Query order_magnet_items table to get orderId:
     SELECT order_id FROM order_magnet_items WHERE activation_code = ?
   - Populate household.orderId field with result
   - This connects household back to purchase

4. SAVE HOME PROFILE DATA
   - Create record in home_profile_extras table
   - Map form fields:
     * homeType → home_type
     * yearBuilt → year_built
     * sqft → square_footage
     * bedrooms → bedrooms
     * bathrooms → bathrooms
     * hvacType → hvac_type
     * waterHeater → water_heater_type
     * roofAgeYears → roof_age
   - Link via householdId as primary key

5. UPDATE QR CODE STATUS
   - Update order_magnet_items table:
     * activation_status = 'active'
     * activated_at = CURRENT_TIMESTAMP
     * activated_by_email = customer email from form
   - WHERE activation_code = token

6. MARK SETUP COMPLETE
   - Set household fields:
     * setup_status = 'complete' (not 'in_progress')
     * setup_completed_at = CURRENT_TIMESTAMP
     * setup_started_at = CURRENT_TIMESTAMP (if not already set)

7. ERROR HANDLING
   - Wrap in try-catch
   - On error: rollback transaction
   - Log error details
   - Return 500 with user-friendly message
   - Don't expose internal errors to client

SCHEMA REFERENCE:

households table:
- id (varchar, PK)
- name, email, phone
- address_line_1, city, state, zipcode
- setup_status (varchar: 'in_progress', 'complete')
- setup_started_at, setup_completed_at (timestamps)
- magnet_token (varchar, unique)
- order_id (varchar, FK to order_magnet_orders)
- created_by, created_by_user_id

home_profile_extras table:
- household_id (varchar, PK, FK to households)
- home_type, year_built, square_footage
- bedrooms, bathrooms
- hvac_type, water_heater_type, roof_age
- heating_fuel, has_gas, has_septic, etc.

order_magnet_items table:
- id (varchar, PK)
- order_id (varchar, FK)
- activation_code (varchar, unique)
- activation_status (varchar: 'inactive', 'active')
- activated_at (timestamp)
- activated_by_email (varchar)

EXISTING CODE LOCATION:
File: server/src/routes/setup.ts
Current endpoint: router.post('/activate', async (req, res) => {...})
Lines: ~50-120

IMPLEMENTATION REQUIREMENTS:
- Use Drizzle ORM (already imported)
- Use existing storage methods where available
- Follow existing code patterns in file
- Maintain backward compatibility
- Return same response structure (add new fields, don't remove old)

TESTING REQUIREMENTS:
After implementation, test:
1. Valid token + complete form → household created with all data
2. Already-used token → 409 error
3. Invalid token → 404 error
4. Database error mid-transaction → rollback, no partial data
5. Verify all tables updated correctly

Provide complete, production-ready code for server/src/routes/setup.ts POST /activate endpoint.
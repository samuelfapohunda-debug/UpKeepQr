# 🚀 QUICK COMMAND REFERENCE - UpKeepQR Admin Credentials

## 📋 ADMIN CREDENTIALS

```
Email:    samuel@upkeepqr.com
Password: UpKeep2025!Admin
URL:      http://localhost:5173/login
```

---

## ⚡ COPY-PASTE COMMANDS FOR REPLIT

### Option 1: One-Line Display (Fastest) ⭐

Copy and paste this entire command into your Replit shell:

```bash
bash /mnt/user-data/outputs/QUICK-ADMIN-SETUP.sh
```

### Option 2: Full Setup Script

For detailed information with instructions:

```bash
bash /home/claude/create-admin-simple.sh
```

### Option 3: Add to Firebase Database

If you need to add the admin to Firebase:

```bash
bash /home/claude/add-admin-to-firebase.sh
```

*(Requires service-account.json in project root)*

---

## 📱 QUICK TEST

After running the auth implementation, test with these steps:

```bash
# 1. Make sure your app is running
# Check backend: http://localhost:5000
# Check frontend: http://localhost:5173

# 2. Open login page
# Navigate to: http://localhost:5173/login

# 3. Enter credentials:
#    Email: samuel@upkeepqr.com
#    Password: UpKeep2025!Admin

# 4. Click "Sign In"
# Should redirect to: /admin/requests
```

---

## 🔍 VERIFY CREDENTIALS ANYTIME

View saved credentials:

```bash
cat /tmp/admin-creds.txt
```

Or display formatted:

```bash
bash /home/claude/create-admin-simple.sh
```

---

## 🛠️ FIREBASE MANUAL SETUP

If scripts don't work, add this to Firebase Console manually:

**Path:** `/agents/agent_samuel`

**JSON Data:**
```json
{
  "agentId": "agent_samuel",
  "email": "samuel@upkeepqr.com",
  "role": "admin",
  "name": "Samuel (Admin)",
  "isActive": true
}
```

---

## 🐛 TROUBLESHOOTING COMMANDS

### Check if backend is running:
```bash
curl http://localhost:5000/api/health
```

### Test login endpoint directly:
```bash
curl -X POST http://localhost:5000/api/agent/login \
  -H "Content-Type: application/json" \
  -d '{"email":"samuel@upkeepqr.com","password":"UpKeep2025!Admin"}'
```

### Check environment variables:
```bash
# Backend
echo $JWT_SECRET

# Frontend
cat client/.env | grep VITE_API_BASE_URL
```

### Check if admin exists in Firebase:
```bash
# Open Firebase Console → Realtime Database
# Look for: /agents/agent_samuel
```

---

## 📄 ALL AVAILABLE FILES

These files have been created for you:

1. **Quick Setup:**
   - `/mnt/user-data/outputs/QUICK-ADMIN-SETUP.sh` (one-liner)
   - `/home/claude/create-admin-simple.sh` (detailed display)

2. **Firebase Setup:**
   - `/home/claude/add-admin-to-firebase.sh` (add to database)
   - `/home/claude/create-admin-credential.sh` (full generator)

3. **Documentation:**
   - `/mnt/user-data/outputs/ADMIN-CREDENTIALS-SETUP.md` (complete guide)
   - `/mnt/user-data/outputs/SPEC-FRONTEND-AUTH-RBAC-FINAL.md` (auth spec)
   - `/mnt/user-data/outputs/QUICK-COMMANDS.md` (this file)

4. **Saved Credentials:**
   - `/tmp/admin-creds.txt` (after running any script)

---

## 🎯 RECOMMENDED WORKFLOW

1. **First Time Setup:**
   ```bash
   # Display credentials
   bash /home/claude/create-admin-simple.sh
   
   # If using Firebase, add admin
   bash /home/claude/add-admin-to-firebase.sh
   ```

2. **Implement Frontend Auth:**
   ```bash
   # Follow the spec document
   # See: SPEC-FRONTEND-AUTH-RBAC-FINAL.md
   # Copy the Replit Agent prompt from the spec
   ```

3. **Test Login:**
   ```bash
   # Visit: http://localhost:5173/login
   # Use credentials above
   # Verify redirect to /admin/requests
   ```

4. **Verify Everything Works:**
   - [ ] Login succeeds
   - [ ] Token stored in localStorage
   - [ ] Admin links visible in navigation
   - [ ] Can access /admin/requests
   - [ ] Can access /admin/magnets
   - [ ] Logout works
   - [ ] Token persists on refresh

---

## 💡 PRO TIPS

**Bookmark These URLs:**
- Login: `http://localhost:5173/login`
- Admin Dashboard: `http://localhost:5173/admin/requests`
- Magnet Orders: `http://localhost:5173/admin/magnets`

**Browser DevTools Shortcuts:**
```javascript
// Check token (in Console)
localStorage.getItem('upkeepqr_admin_token')

// Check if authenticated
localStorage.getItem('upkeepqr_admin_email')

// Clear and re-login
localStorage.clear()
window.location.href = '/login'
```

**Password Info:**
- Strong: 16 characters
- Mix: Upper, lower, numbers, special chars
- Dev only: Change in production!

---

## 🔐 SECURITY REMINDER

⚠️ **IMPORTANT:** This password is for **development/testing only**

**In Production:**
- [ ] Change the password immediately
- [ ] Use environment variables
- [ ] Enable password hashing (bcrypt)
- [ ] Implement 2FA
- [ ] Use secrets manager
- [ ] Rotate credentials regularly

---

**Ready to use!** 🎉

Run the quick command above to get started.
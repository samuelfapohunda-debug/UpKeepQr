╔══════════════════════════════════════════════════════════════╗
║  🚨 REPLIT AGENT INSTRUCTION - READ CAREFULLY               ║
║  Home Setup Form Styling Update with CRITICAL Warnings      ║
╚══════════════════════════════════════════════════════════════╝

🚨🚨🚨 CRITICAL WARNING - READ THIS FIRST 🚨🚨🚨
═══════════════════════════════════════════════════════════════

DO NOT BREAK GOOGLE ADDRESS AUTO-POPULATION FUNCTIONALITY

The Street Address field has Google Places API integration that automatically
fills City, State/Province, and ZIP/Postal Code when user selects an address.

YOU MUST PRESERVE THIS FUNCTIONALITY OR THE FORM WILL BE BROKEN.

═══════════════════════════════════════════════════════════════

📋 REPLIT AGENT INSTRUCTION

Task: Update "Complete Your Home Setup" form styling to match Request a Pro form
File: Likely client/src/pages/NewSetup.tsx or similar
Spec: REPLIT_HOME_SETUP_FORM_UNIFORMITY.txt (full details)

🚨 MANDATORY: PRESERVE GOOGLE ADDRESS AUTOCOMPLETE 🚨
────────────────────────────────────────────────────────────

The Street Address field uses Google Places API to auto-populate:
• City field (auto-fills from selected address)
• State/Province field (auto-fills from selected address)
• ZIP/Postal Code field (auto-fills from selected address)

DO NOT MODIFY:
❌ Address autocomplete initialization code
❌ Google Places event handlers (onPlaceChanged, etc.)
❌ Auto-fill logic for City/State/ZIP fields
❌ Address parsing functions
❌ Field refs (addressRef or similar)
❌ Field name attributes
❌ Field id attributes (if used by autocomplete)
❌ React event handlers (onChange, onSelect, etc.)
❌ State management for address fields

ONLY CHANGE:
✅ CSS/Tailwind classes for styling ONLY
✅ Remove placeholder text ("Start typing your address...")
✅ Add label above field ("Street Address *")
✅ Background colors (bg-white)
✅ Border styles (border-gray-300)
✅ Focus states (focus:ring-2 focus:ring-blue-500)

═══════════════════════════════════════════════════════════════

✅ STYLING CHANGES TO MAKE

Step 2: Account Setup (Main Focus)
────────────────────────────────────────────────────────────

1. Form Container Background
   FROM: White or blue background
   TO: Light gray (bg-gray-50 or #f9fafb)
   Add: p-10 rounded-lg max-w-2xl mx-auto

2. Remove In-Field Placeholders
   FROM: placeholder="you@example.com"
   FROM: placeholder="John Smith"
   FROM: placeholder="Start typing your address..."
   FROM: placeholder="12345 or A1B 2C3"
   TO: placeholder="" (empty string)

3. Add Labels Above All Fields
   Add above EACH input:
   ```tsx
   <label className="block text-sm font-semibold text-gray-700 mb-2">
     Email Address *
   </label>
   ```
   
   Labels needed:
   • "Email Address *"
   • "Full Name *"
   • "Street Address *" ← CRITICAL: Keep autocomplete working
   • "City" (auto-fill field)
   • "State / Province" (auto-fill field)
   • "ZIP / Postal Code *"

4. Input Field Styling
   Update all inputs to:
   className="w-full bg-white border border-gray-300 rounded-md px-4 py-3 text-base text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"

5. Keep Helper Text Below Fields
   MAINTAIN these helper texts:
   • "We'll send your schedule and reminders here"
   • "Start typing for suggestions (US & Canada)"
   • "Auto-fills from address" (for City/State)
   • "For weather-based reminders and local service matching"

6. Continue Button Styling
   className="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 px-6 rounded-md transition-colors duration-200"

7. Auto-Fill Fields (City, State)
   Keep as disabled
   Add white background (same as other fields)
   className="w-full bg-white border border-gray-300 rounded-md px-4 py-3 text-base text-gray-500 cursor-not-allowed"

═══════════════════════════════════════════════════════════════

🚨 STREET ADDRESS FIELD - SPECIAL ATTENTION 🚨

EXAMPLE OF CORRECT APPROACH:

BEFORE:
```tsx
<input
  ref={addressRef}  // ← Google Places reference
  name="streetAddress"
  placeholder="Start typing your address..."
  onChange={handleAddressChange}
  onFocus={initializeAutocomplete}
  className="form-input"
/>
```

AFTER (CORRECT):
```tsx
<label className="block text-sm font-semibold text-gray-700 mb-2">
  Street Address *
</label>
<input
  ref={addressRef}  // ← KEEP THIS - DO NOT REMOVE
  name="streetAddress"  // ← KEEP THIS - DO NOT CHANGE
  onChange={handleAddressChange}  // ← KEEP THIS - DO NOT REMOVE
  onFocus={initializeAutocomplete}  // ← KEEP THIS IF IT EXISTS
  placeholder=""  // ← ONLY CHANGE: Remove text, keep attribute
  className="w-full bg-white border border-gray-300 rounded-md px-4 py-3 text-base text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500"  // ← ONLY CHANGE: Update styling
/>
<p className="text-sm text-gray-600 mt-2">
  Start typing for suggestions (US & Canada)
</p>
```

WHAT YOU CHANGED:
✅ Added label above field
✅ Removed placeholder text
✅ Updated className for styling
✅ Kept helper text below

WHAT YOU DID NOT CHANGE:
✅ ref attribute (critical for Google Places)
✅ name attribute
✅ onChange handler
✅ onFocus handler (if exists)
✅ Any other event handlers

═══════════════════════════════════════════════════════════════

🧪 MANDATORY TESTING AFTER IMPLEMENTATION

YOU MUST TEST THIS BEFORE CONSIDERING THE TASK COMPLETE:

1. Navigate to /new-setup in browser
2. Progress to Step 2 (Account Setup)
3. Click on Street Address field
4. Type: "1600 Pennsylvania Ave"
5. Wait for Google autocomplete dropdown to appear
6. Select: "1600 Pennsylvania Avenue NW, Washington, DC 20500"
7. VERIFY:
   ✓ City field auto-fills with "Washington"
   ✓ State field auto-fills with "DC" or "District of Columbia"
   ✓ ZIP field auto-fills with "20500"

IF ANY OF THE ABOVE FAILS:
🚨 YOU BROKE THE AUTOCOMPLETE 🚨
REVERT ALL CHANGES IMMEDIATELY
DO NOT PROCEED UNTIL AUTOCOMPLETE WORKS

Alternative Test Addresses:
• "1 Apple Park Way, Cupertino, CA"
• "350 Fifth Avenue, New York, NY"
• "1600 Amphitheatre Parkway, Mountain View, CA"

═══════════════════════════════════════════════════════════════

⚠️  COMMON MISTAKES THAT BREAK AUTOCOMPLETE

DON'T DO THESE:
❌ Removing ref={addressRef} or ref={autocompleteRef}
❌ Changing name="streetAddress" to something else
❌ Removing onChange handler
❌ Wrapping input in a new div that breaks event propagation
❌ Adding disabled attribute to Street Address field
❌ Changing input type from "text" to something else
❌ Making Street Address a controlled component incorrectly
❌ Removing field id if it's used by Google Places
❌ Modifying Google Places initialization code
❌ Removing or modifying useEffect that sets up autocomplete
❌ Changing state variable names used by autocomplete

═══════════════════════════════════════════════════════════════

✅ WHAT TO KEEP UNCHANGED

Multi-Step Wizard:
────────────────────────────────────────────────────────────
• Progress indicator (Steps 1-4)
• Progress bar
• Step numbers
• Current step highlighting
• Step navigation (forward/back buttons)
• Step transition logic
• Form data persistence between steps

Google Autocomplete:
────────────────────────────────────────────────────────────
• Google Places API script loading
• API key configuration
• Autocomplete initialization
• Address parsing logic
• Auto-fill functions
• Event handlers for address selection
• State updates for City/State/ZIP

Helper Text:
────────────────────────────────────────────────────────────
• "We'll send your schedule and reminders here"
• "Start typing for suggestions (US & Canada)"
• "Auto-fills from address"
• "For weather-based reminders and local service matching"

═══════════════════════════════════════════════════════════════

📋 STEP-BY-STEP IMPLEMENTATION

1. Find the file (likely: client/src/pages/NewSetup.tsx)

2. Update form container:
   Add: bg-gray-50 p-10 rounded-lg max-w-2xl mx-auto

3. For EACH field (Email, Full Name, Street Address, etc.):
   a. Add label above with className="block text-sm font-semibold text-gray-700 mb-2"
   b. Remove placeholder text (change to placeholder="")
   c. Update input className to white background with gray border
   d. Keep helper text below (if it exists)

4. For Street Address specifically:
   ⚠️  DO NOT touch ref, name, onChange, or any handlers
   ⚠️  ONLY change className and remove placeholder text
   ⚠️  Test autocomplete immediately after

5. For City/State (disabled fields):
   Update to white background with gray text

6. Update Continue button to blue styling

7. TEST GOOGLE AUTOCOMPLETE:
   Type address → Select from dropdown → Verify auto-fill works

8. If autocomplete broken: REVERT AND START OVER

═══════════════════════════════════════════════════════════════

🎨 COLOR REFERENCE

Use these exact colors:

Form Container:    bg-gray-50 (#f9fafb)
Input Background:  bg-white (#ffffff)
Border:            border-gray-300 (#d1d5db)
Label:             text-gray-700 (#374151)
Helper Text:       text-gray-600 (#6b7280)
Focus Ring:        focus:ring-blue-500 (#3b82f6)
Button:            bg-blue-500 (#3b82f6)
Button Hover:      hover:bg-blue-600 (#2563eb)

═══════════════════════════════════════════════════════════════

✅ ACCEPTANCE CRITERIA

Visual:
□ Form container has light gray background
□ All inputs have white backgrounds
□ Labels visible above each field
□ No placeholder text inside fields
□ Helper text below fields (maintained)
□ Matches Request a Pro form styling

Functionality:
□ Multi-step navigation works
□ Can type in all fields
□ Form validation works
□ Continue button works

🚨 CRITICAL - Google Autocomplete:
□ Can type in Street Address field
□ Google autocomplete dropdown appears
□ Can select address from dropdown
□ City auto-fills correctly ✓
□ State auto-fills correctly ✓
□ ZIP auto-fills correctly ✓

IF AUTOCOMPLETE FAILS: TASK IS NOT COMPLETE

═══════════════════════════════════════════════════════════════

📞 IF YOU HAVE QUESTIONS

Q: Can I refactor the address autocomplete code?
A: NO. Only change styling (className). Do not touch logic.

Q: Can I change the field names?
A: NO. Keep all name attributes exactly as they are.

Q: Can I wrap inputs in new divs?
A: YES, but be careful not to break event handlers or refs.

Q: What if autocomplete uses a library like react-places-autocomplete?
A: Follow the library's requirements. Keep all required props.

Q: Can I change placeholder from "text" to ""?
A: YES. That's required. Remove placeholder text but keep the attribute.

Q: What if I break autocomplete by accident?
A: Immediately revert all changes and start over more carefully.

═══════════════════════════════════════════════════════════════

🚨 FINAL WARNING 🚨

The Google address autocomplete is CRITICAL functionality.
Users depend on it to quickly fill in their address information.

If you break it:
• Users cannot complete the setup wizard
• City/State/ZIP must be manually entered
• User experience is significantly degraded
• The form becomes much harder to use

Take your time. Be careful. Test thoroughly.

ONLY change visual styling.
DO NOT modify any JavaScript logic or event handlers.

═══════════════════════════════════════════════════════════════

✅ YOU ARE READY TO PROCEED

Read the full specification: REPLIT_HOME_SETUP_FORM_UNIFORMITY.txt
Follow the code examples carefully
Test autocomplete after EVERY change to Street Address field
Revert immediately if autocomplete breaks

Good luck! 🚀

═══════════════════════════════════════════════════════════════

END OF INSTRUCTION